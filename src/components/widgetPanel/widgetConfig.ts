// 容器
import { generateId } from '@/utils/util'

export const containers = [
  {
    type: 'section',
    tagName: 'mj-section',
    attributes: {}, //
    children: [],
    id: generateId(),
  },
]

// 基础组件
export const basicWidget = []
